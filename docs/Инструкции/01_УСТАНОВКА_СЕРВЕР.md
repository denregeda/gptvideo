# 01. Установка и настройка сервера

Сервер — это Docker-стек из 7 контейнеров: PostgreSQL, Redis, MinIO (хранилище
медиа), API (FastAPI), Celery (фоновые задачи), Nginx (HTTPS на порту 443),
NTP (раздаёт точное время мини-ПК). Ставится одной командой.

---

## 1. Требования

- 64-битный Linux-сервер (или ВМ) с доступом по SSH.
- **Docker Engine** и **Docker Compose v2** (команда `docker compose`, не `docker-compose`).
  Проверка: `docker --version` и `docker compose version`.
- Открытые **443/tcp** (HTTPS/WSS), **80/tcp** (переход/редирект) и
  **123/udp** (NTP).
- Желательно — выход в интернет для синхронизации времени с российскими
  серверами (ВНИИФТРИ, MSK-IX). В изолированной сети время раздаёт сам сервер.
- Свободное место под медиа и БД (том MinIO + том Postgres).

Если Docker не установлен: https://docs.docker.com/engine/install/

---

## 2. Установка (одна команда)

Из каталога проекта:

```bash
cd Video_miniPC_v16.2_modular
bash install_server.sh
```

Скрипт `install_server.sh`:
1. Проверяет Docker и Compose v2.
2. Создаёт `.env` со **случайными** секретами: пароль БД, пароль MinIO,
   `SECRET_KEY` для подписи токенов входа.
   > Существующие пароли сохраняются. Единственное исключение: короткий,
   > шаблонный или явно слабый `SECRET_KEY` автоматически заменяется; старые
   > панели разлогинятся, данные и пароли пользователей не изменятся.
3. Создаёт/проверяет локальный CA и TLS-сертификат nginx.
4. Собирает и поднимает стек: `docker compose up -d --build`.
   Миграции БД (`001`–`033`) применяются автоматически при первом старте Postgres.
5. Проверяет HTTPS без `-k`, ждёт готовности API и печатает адрес панели.

По окончании увидите адрес панели и **сгенерированный стартовый пароль
администратора** (показывается ОДИН раз — запишите его):
```
Панель:  https://10.0.119.100/
Вход:    admin / <случайный пароль, показан установщиком>
```
> При первой установке пароль `admin` генерируется случайным и печатается в конце
> установки. При первом входе панель всё равно потребует задать свой новый пароль.

> Только сгенерировать `.env` без запуска стека: `bash install_server.sh --env-only`.

---

## 3. Первый вход и смена пароля администратора ⚠️

1. Откройте в браузере `https://10.0.119.100/`.
2. Войдите: **admin** и пароль, напечатанный установщиком в конце установки.
3. Панель **сразу потребует задать новый пароль** — задайте свой надёжный.

> Если по какой-то причине доступ утрачен — сбросить пароль admin на новый
> случайный можно из каталога проекта:
> `docker compose exec -T api python /app/reset_admin_password.py`
> (напечатает новый пароль; при входе панель снова попросит его сменить).

---

## 4. Файл `.env`

Лежит в корне проекта, права `600`, генерируется установщиком. Содержит:

```
POSTGRES_DB=display_system
POSTGRES_USER=display_user
DB_PASSWORD=<случайный>
MINIO_USER=<...>
MINIO_PASSWORD=<случайный>
SECRET_KEY=<случайный, 64 hex>
AUTH_ACCOUNT_FAILURE_LIMIT=5
AUTH_IP_FAILURE_LIMIT=20
AUTH_FAILURE_WINDOW_SECONDS=900
TLS_SERVER_NAME=display.local
TLS_EXTRA_SANS=
TLS_RENEW_DAYS=30
TLS_LEGACY_HTTP=true
```

- **Не публикуйте** этот файл и **не копируйте** его между серверами.
- `SECRET_KEY` подписывает токены входа — при его смене все текущие сессии
  разлогинятся.
- Параметры `AUTH_*` задают лимиты ошибок на учётку/IP и окно в секундах.
  При недоступном Redis новый вход закрывается с `503`, но эфир не прерывается.
- Новые миграции БД применяются автоматически: на чистой установке — через
  `initdb` (список в `docker-compose.yml`), на обновлении — через `migrate.sh`
  (вызывается из `install_server.sh`). Достаточно положить `NNN_*.sql` в
  `server/migrations/`.

---

## 5. Проверка после установки

```bash
cd Video_miniPC_v16.2_modular
docker compose ps                    # все 7 контейнеров: running
curl --cacert tls/generated/ca.crt https://localhost/health  # 200 OK
docker exec ds_ntp chronyc sources   # NTP видит серверы времени
```

Затем откройте панель `https://10.0.119.100/` и войдите.
Порядок миграции экранов: [10_HTTPS_И_ПЕРЕХОД.md](10_HTTPS_И_ПЕРЕХОД.md).

---

## 6. Настройка MAX-уведомлений (необязательно)

Система умеет слать тревоги (экран офлайн, мало места, нерабочий ролик) в
мессенджер MAX. Настраивается в панели, код не трогаем.

**Настройки → Уведомления:**
1. Включите «Уведомления включены».
2. Впишите **токен бота** и **адрес** (endpoint API MAX).
3. Задайте порог «Экран офлайн дольше (мин)».
4. Отметьте, какие события слать: *Экран офлайн*, *Мало места*, *Нерабочий ролик*,
   *Монитор отключён* (мини-ПК работает и играет, но кабель HDMI/DisplayPort
   отсоединён — тревога поднимается только по живым экранам и не раньше чем
   через 3 минуты после отключения, чтобы не срабатывать на перезагрузку
   монитора).
5. **Сохранить** → **Отправить тест** (придёт тестовое сообщение).

> Заголовок авторизации к API MAX — `Authorization` **без** префикса `Bearer`
> (сверено с офиц. документацией MAX).

---

## 7. Точное время (NTP)

Контейнер `ds_ntp` (chrony) синхронизируется с российскими эталонами времени и
сам раздаёт время мини-ПК. Это важно: расписание и отчёты по показам считаются
по **московскому времени**, экраны должны идти секунда-в-секунду.

Проверка: `docker exec ds_ntp chronyc sources` и `... chronyc tracking`.

### Часы хоста ⚠️

Контейнеры живут по часам **хоста**, а `ds_ntp` их только *измеряет*, но не
правит. Если на хосте нет службы времени, часы уплывают — и всё расписание,
журнал показов и счета едут вместе с ними (реальный случай: сервер жил на
−3 часа, «NTP service: inactive»). Обязательно настройте синхронизацию:

```bash
sudo apt install -y chrony
printf 'server ntp.ix.ru iburst\nserver ntp1.vniiftri.ru iburst\nserver ntp2.vniiftri.ru iburst\nmakestep 1 -1\n' | sudo tee /etc/chrony/sources.d/ds.sources >/dev/null 2>&1 \
  || printf 'server ntp.ix.ru iburst\nserver ntp1.vniiftri.ru iburst\nmakestep 1 -1\n' | sudo tee -a /etc/chrony/chrony.conf >/dev/null
sudo systemctl enable --now chrony 2>/dev/null || sudo systemctl enable --now chronyd
chronyc tracking          # System time — доли секунды
```

Также рекомендуем `sudo timedatectl set-local-rtc 0` (RTC в UTC).

> Контроль постоянный: смещение часов хоста видят **Настройки → Диагностика
> сервера** (пункт NTP) и `selfheal.sh` — оба подскажут команду лечения.
> Плюс `install_server.sh` сверяет часы ещё в preflight, **до** сборки: сбитое
> время роняет установку на проверке подписей apt («Release file is not yet
> valid»), причём сообщение об ошибке на время никак не намекает.
> Быстрая ручная подводка без chrony:
> ```bash
> OFF=$(docker exec ds_ntp chronyc tracking | awk '/System time/{printf "%d", ($6=="fast")? -$4 : $4}'); sudo date -s "now $OFF seconds"
> ```

---

## 8. Бэкапы и восстановление

Автобэкапы БД и медиа (MinIO) выполняет Celery по расписанию; архивы — в `backups/`.
Порядок **восстановления** из бэкапа подробно описан в
[../RESTORE_GUIDE.md](../RESTORE_GUIDE.md) — процедура проверена.

Разовый ручной бэкап и управление — через **Настройки** и раздел бэкапов в
панели под ролью `admin`/`superadmin`. API запрещает список, скачивание,
создание и удаление бэкапов остальным ролям; скачивание и удаление попадают
в журнал операций.

---

## 9. Обновление сервера

Повторный запуск установщика безопасно пересобирает контейнеры, сохраняя `.env`,
БД и медиа:

```bash
cd Video_miniPC_v16.2_modular
git pull            # если репозиторий под git
bash install_server.sh
```

---

## 10. Полное удаление

> **⚠️ `-v` удаляет тома с БД и медиа безвозвратно.** Сначала сделайте бэкап.
> Подробно — в [../CLEANUP_FULL_2026-07-01.md](../CLEANUP_FULL_2026-07-01.md).

```bash
docker compose down -v
```

---

## Диагностика

| Симптом | Что сделать |
|---|---|
| API не поднялся | `docker compose logs api` |
| Контейнер не `running` | `docker compose ps`, затем `docker compose logs <имя>` |
| Панель не открывается | `bash tls/manage_tls.sh check`, проверьте 443 и `docker compose logs nginx` |
| Время «плывёт» | `docker exec ds_ntp chronyc tracking` |
| Забыли пароль admin | смена — через пользователя-`superadmin` в разделе Пользователи |
| Сборка падает: «not signed / Not live until …» | **Часы сервера отстают** → apt не проверяет подписи. Выставьте верное время: `sudo timedatectl set-ntp true` (или вручную `sudo timedatectl set-time "ГГГГ-ММ-ДД ЧЧ:ММ:СС"`), затем повторите `bash install_server.sh`. |
| MinIO падает: «CPU does not support x86-64-v2» | Старый CPU. В `docker-compose.yml` образ MinIO уже зафиксирован на совместимый релиз (`RELEASE.2022-10-24…`). Если всё ещё падает — возьмите ещё более старый релиз MinIO. |

Следующий шаг — [установка агента на мини-ПК](02_УСТАНОВКА_АГЕНТ.md).
