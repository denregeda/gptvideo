# 04. Тесты

В проекте три уровня проверки перед релизом: **регрессионные unit-тесты**,
**smoke-тест** против живого стека и браузерный **UI/E2E-прогон**.

---

## 1. Регрессионные тесты (pytest) — `server/tests/`

### Что это и зачем
Автотесты, которые защищают **«денежную» и учётную математику** от случайных
поломок при будущих правках:
- **биллинг** — расчёт суммы счёта по тарифу (за показы / за минуты), округление
  до копеек, разбор дат периода, преобразование объёмов из `play_log`, итог с
  учётом скидки/доплаты;
- **кампании** — план/факт (план = цель × прошедшие дни), процент выполнения,
  недобор, минуты фактического эфира, переходы статусов
  (scheduled/active/finished/paused), границы дат по МСК, дневная детализация;
- **индивидуальные условия кампании** — тариф за показ/минуту, цена,
  ручная скидка, округление через `Decimal`, защита от отрицательного итога и
  разрешённый маршрут подачи заявки рекламодателем;
- **контроль монитора** — разбор `/sys/class/drm/*/status` агентом (в том числе
  «статус неизвестен» и виртуальные выходы, которые нельзя принимать за
  отключённый монитор) и правило тревоги на сервере;
- **безопасность офлайн-кеша** — сетевой сбой и некорректный манифест не
  превращаются в пустой список, синхронизация пропускается без падения агента,
  а локальные ролики не удаляются;
- **атомарная загрузка агента** — обрыв передачи, неверный MD5, короткий ответ,
  корректный resume и сервер, игнорирующий Range, не повреждают рабочий ролик;
- **защита бэкапов** — аноним, рекламодатель, аудитор и модератор не могут
  скачать полный дамп БД; администратор может, действие журналируется, а имя
  файла из БД не позволяет выйти за пределы каталога бэкапов;
- **fail-closed авторизация** — ошибка PostgreSQL не подставляет роль
  администратора; удаление, блокировка и смена роли действуют для уже
  выданного JWT; HTTP middleware и WebSocket закрываются безопасно; права
  записи и модерации используют явные белые списки ролей;
- **карточка рекламодателя** — границы московских суток в UTC: ошибка на три
  часа тихо перенесла бы показы соседних суток в чужой период, а значит и в
  чужой счёт и эфирную справку.

Раньше это проверялось только вручную при каждой доработке. Теперь одна команда
подтверждает, что формулы не «поехали».

### Как устроено
Тесты **не требуют реального Postgres**: сессия БД подменяется лёгким моком
(`FakeDB` в [`conftest.py`](../../server/tests/conftest.py)), который возвращает
заранее заданные строки. Проверяется именно **чистая математика** над этими
данными. Корректность самих SQL-запросов проверяет smoke-тест против живого
стека (см. §2) — это разные уровни, они дополняют друг друга.

Файлы:
| Файл | Что покрывает |
|---|---|
| `server/tests/conftest.py` | Мок БД (`FakeDB`), фикстура `fake_db`, настройка окружения |
| `server/tests/test_billing.py` | `_calc_amount`, `_parse_date`, `_period_stats`, итог со скидкой |
| `server/tests/test_campaigns.py` | `_campaign_facts` (план/факт/статусы/границы), `_parse_date` |
| `server/tests/test_campaign_pricing.py` | Индивидуальный тариф, цена, скидка, округление и доступ к заявкам |
| `server/tests/test_display_signal.py` | Разбор `/sys/class/drm/*/status` агентом и правило тревоги «монитор отключён» |
| `server/tests/test_agent_cache_safety.py` | Защита локального медиакеша при ошибках сети/манифеста |
| `server/tests/test_agent_download_atomic.py` | `.part`, resume, размер/MD5 и атомарная публикация ролика |
| `server/tests/test_file_delivery.py` | Разбор открытых, ограниченных, suffix и некорректных HTTP Range |
| `server/tests/test_backup_access.py` | Роли, обязательная авторизация, аудит скачивания и защита пути бэкапа |
| `server/tests/test_auth_fail_closed.py` | Сбой БД, удаление/блокировка/смена роли, HTTP/WS и белые списки прав |
| `server/tests/test_advertiser_office.py` | Границы московских суток в UTC для карточки рекламодателя, валидация периода |
| `server/tests/test_advertiser_docs.py` | Нумерация документов и шапка с реквизитами сторон |

### Как запускать
Одной командой из каталога проекта:

```bash
bash run_tests.sh
```

Скрипт [`run_tests.sh`](../../run_tests.sh) сам ставит dev-зависимости (`pytest`)
в работающий контейнер `ds_api` и прогоняет `server/tests`. Запускать можно
**после любого обновления/пересборки** — воспроизводимо.

`pytest` **не входит** в production-образ (он не нужен для работы системы) —
dev-зависимости вынесены в
[`server/requirements-dev.txt`](../../server/requirements-dev.txt). Эквивалент
вручную:

```bash
docker exec ds_api pip install -r /app/requirements-dev.txt
docker exec -w /app ds_api python -m pytest tests -q
```

Ожидаемый результат — все тесты зелёные, например:
```
........................................................................ [ 55%]
..........................................................               [100%]
130 passed
```

> Установка `pytest` в контейнер **эфемерна** — теряется при пересборке образа
> (`docker compose up --build`). Это норма: перед прогоном просто повторите шаг 1.

> Настройки pytest — в `server/pytest.ini` (там же глушится постороннее
> предупреждение passlib про устаревший модуль `crypt` в Python 3.11+ — к нашему
> коду оно отношения не имеет).

### Когда запускать
Перед каждым релизом и после любых правок в `routers/billing.py`,
`routers/campaigns.py`, `deps.py`, `auth_identity.py`, middleware или
WebSocket. Быстро (доли секунды) — можно гонять хоть на каждый коммит.

---

## 2. Smoke-тест — `smoke_test.sh`

Сквозной прогон **против живого стека**: проверяет, что все 7 контейнеров живы,
работает вход, здоровье API, реальная докачка медиа (`Range` → HTTP 206 и
точное число байтов) и полный цикл «экран → медиа → плейлист → расписание»,
проверяет запрет скачивания бэкапа без входа и доступ списка бэкапов
администратору, а также доступность модулей (дашборд, отчёты, биллинг,
кампании, модерация, уведомления). Все тестовые данные создаются с префиксом
`SMOKE_` и удаляются в конце.

```bash
cd Video_miniPC_v16.2_modular
bash smoke_test.sh
```

Код возврата `0` — можно выпускать; иначе число упавших проверок.

---

## 3. UI/E2E-тест панели — `ui_test.sh`

### Зачем отдельный уровень
Главный вывод пилотного развёртывания: **почти все дефекты панели нашлись
только живым кликом**. Кнопка звала неверный HTTP-метод (405 при создании
экрана), падала в JS или молча ничего не делала — а `smoke_test.sh` при этом
был зелёный, потому что проверяет эндпоинты, а не интерфейс.

### Что проверяет
1. Вход в панель под администратором.
2. Открытие **всех 14 разделов** левого меню (заголовок сменился, вью не
   отдало «Ошибка: …»).
3. Нажатие **всех «безопасных» кнопок** каждого раздела — открыть форму,
   вкладку, фильтр, отчёт, модалку. Список действий **белый и перечислен
   поимённо** в `ui_tests/ui_test.js` (`SAFE_ACTIONS`): удаление, отправка
   форм, включение эфира, OTA, команды экрану и выгрузки файлов не нажимаются
   никогда. **Добавили новую кнопку — допишите её в список**, иначе она в
   прогон не попадёт.
4. Полный цикл с уборкой за собой: **экран** (создать → токен и ID показаны →
   копирование токена → виден в списке → удалить) и **плейлист** (создать
   через `prompt` → виден → удалить). Тестовые записи именуются `E2E_…`.
5. **Карточка рекламодателя**: открывается и все шесть вкладок (обзор,
   эфир, качество доставки, ролики, заявки, документы) рисуются без ошибок.
6. **Изоляция роли «рекламодатель»** — самая дорогая ошибка раздела. Тест
   заводит настоящую учётку с ролью `advertiser`, входит ею в отдельном
   окне браузера и проверяет: в меню только его кабинет; `/advertisers`,
   `/minipc`, `/billing/invoices`, `/users` и чужая карточка отдают 403;
   свой кабинет открывается. До входа дополнительно проверяются две стандартные
   папки и переименование рекламодателя через панель. Учётка и её кабинет
   удаляются в конце.
7. Индикатор монитора (бейдж 📺) отрисован на карточках экранов.

Безопасный клик **«₽ Условия»** отдельно проверяет наличие модальной формы с
полями тарифа, цены, скидки и основания; системный `prompt()` не используется.

Провалом считается не только упавшая проверка, но и **любая ошибка в консоли
браузера, необработанное JS-исключение или ответ HTTP ≥400** — именно так
выглядели дефекты, которые ловили руками.

### Как запускать
```bash
bash ui_test.sh
```

Chromium на сервер не ставится: прогон идёт в одноразовом контейнере
официального образа puppeteer, подключённом к сети стека (панель видна как
`http://nginx`). Первый запуск скачивает образ (~1 ГБ), дальше — из кеша.
Переменные: `ADMIN_USER` (по умолч. `admin`), `ADMIN_PASS` (`admin123`),
`DS_URL`, `UI_TEST_IMAGE`.

Ожидаемый результат:
```
 Проверок пройдено: 49, провалено: 0, ошибок в браузере: 0
```
Код возврата `0` — можно выпускать; `1` — в выводе перечислены шаг и причина.

---

## Итог: чем пользоваться
| Инструмент | Уровень | Нужен живой стек | Скорость |
|---|---|---|---|
| `pytest` (server/tests) | Юнит: математика формул | Нет (БД замокана) | доли секунды |
| `smoke_test.sh` | Сквозной: API + БД + цикл эфира | Да | секунды |
| `ui_test.sh` | Интерфейс: разделы, кнопки, циклы CRUD | Да | ~2–3 минуты |

Полноценная проверка перед релизом — **все три**: сначала `pytest` (быстро
ловит регресс в формулах), затем `smoke_test.sh` (живая система отрабатывает
цикл целиком), затем `ui_test.sh` (панель кликается и не сыплет ошибками).
