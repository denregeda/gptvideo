-- ============================================================
-- Миграция 026: авто-снятие бегущей строки при окончании показа
-- ------------------------------------------------------------
-- Сервер запоминает, какой плейлист экран играет сейчас
-- (заполняется в /schedule/minipc при каждом опросе агента).
-- При смене плейлиста или команде «Стоп» активные бегущие строки
-- снимаются автоматически — задание не «висит» после окончания
-- показа, при котором его запускали.
-- Идемпотентна.
-- ============================================================

ALTER TABLE screens ADD COLUMN IF NOT EXISTS last_playlist_id INTEGER;

-- ============================================================
-- Конец миграции 026
-- ============================================================
