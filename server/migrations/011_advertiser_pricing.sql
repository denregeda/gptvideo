-- ============================================================
-- Digital Signage — Вариант Б (Мини ПК)
-- Миграция 011: стоимость 1 минуты эфирного времени у рекламодателя
-- ------------------------------------------------------------
-- Идемпотентна. Применяется ПОСЛЕ 010_version_monitor.sql.
-- Заработок считается по Способу 1 (по прокрутам):
--   показы × длительность ролика (мин) × price_per_minute.
-- ============================================================

ALTER TABLE advertisers ADD COLUMN IF NOT EXISTS price_per_minute NUMERIC(12,2) DEFAULT 0;

-- ============================================================
-- Конец миграции 011
-- ============================================================
