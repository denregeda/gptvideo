-- ============================================================
-- Digital Signage — Вариант Б (Мини ПК)
-- Миграция 005: переименование роли analyst → auditor
-- ------------------------------------------------------------
-- Идемпотентна. Применяется ПОСЛЕ 004_users_roles.sql.
-- Роль «Аудитор» (auditor): право на просмотр всех разделов и отчётов,
-- без внесения изменений (изменяющие операции запрещены на сервере).
-- ============================================================

UPDATE users SET role = 'auditor' WHERE role = 'analyst';

-- ============================================================
-- Конец миграции 005
-- ============================================================
